# 团队贡献规范

## 分支流程

1. 从最新 `main` 创建功能分支。
2. 分支名称使用 `feature/<name>`、`fix/<name>` 或 `docs/<name>`。
3. 在功能分支完成开发、测试和文档更新。
4. 推送分支并创建 Pull Request。
5. 至少一名团队成员审查通过后合并到 `main`。

不要直接在共享 `main` 分支上开发。仓库创建后建议启用分支保护，要求 Pull Request 和测试通过后才能合并。

## 本地检查

```powershell
python -m compileall app tests
python -m pytest -q
```

提交前还应在浏览器验证受影响的页面和操作流程。

## 提交信息

建议使用以下前缀：

- `feat:` 新功能
- `fix:` 缺陷修复
- `docs:` 文档修改
- `test:` 测试修改
- `refactor:` 不改变行为的重构
- `chore:` 工具或维护工作

示例：`feat: add historical collection date range`

## 代码要求

- 使用 Python 3.12，并为公开函数和复杂数据结构添加类型标注。
- 保持模块职责清晰，采集逻辑不要直接写入 API 路由。
- 数据库结构变更必须提供兼容现有数据库的迁移逻辑。
- 新增采集规则时必须覆盖成功、失败和边界条件测试。
- 前端新增字段时同时处理新增、编辑、加载和错误状态。
- 不提交数据库备份、日志、虚拟环境、密钥或真实业务数据。

## Pull Request 内容

Pull Request 应说明：

- 修改目的和业务背景。
- 主要实现方式。
- 验证步骤与测试结果。
- 数据库或配置兼容性影响。
- 页面变化截图（涉及 UI 时）。

## 安全与数据

RSS 地址、代理、Token 和部署密码不得硬编码。后续引入密钥时使用环境变量，并只提交不含真实值的 `.env.example`。
